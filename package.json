{
  "name": "rate-limiter-service",
  "version": "1.0.0",
  "description": "Token bucket and sliding window log rate limiters, built from scratch to understand them cold.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
